'use client';import {useEffect} from 'react';import {useRouter} from 'next/navigation';import {useStore} from '@/lib/store';export default function Guard({children}:{children:React.ReactNode}){const{logged}=useStore();const r=useRouter();useEffect(()=>{if(!logged)r.replace('/login')},[logged,r]);if(!logged)return <div className="loading">Checking login…</div>;return <>{children}</>}
