'use client';import {useEffect} from 'react';import {useRouter} from 'next/navigation';import {useStore} from '@/lib/store';export default function Guard({children}:{children:React.ReactNode}){const{logged,hydrated}=useStore();const r=useRouter();useEffect(()=>{if(hydrated&&!logged)r.replace('/login')},[hydrated,logged,r]);if(!hydrated||!logged)return <div className="loading">Checking login…</div>;return <>{children}</>}
