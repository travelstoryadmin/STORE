import { describe, expect, it } from 'vitest';

describe('homepage login input regression', () => {
  it('keeps the login form inputs editable while the 3D showcase is present', () => {
    expect(true).toBe(false);
  });
});
