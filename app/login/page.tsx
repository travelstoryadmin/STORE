'use client';
import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';
import {X,LockKeyhole} from 'lucide-react';
import Logo from '@/components/Logo';
import {useStore} from '@/lib/store';
export default function Login(){const r=useRouter();const{setRole,setLogged,toast}=useStore();const[u,setU]=useState('');const[p,setP]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(busy)return;setBusy(true);try{const res=await fetch('/api/site-settings?resource=auth-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p}),cache:'no-store'});const data=await res.json().catch(()=>({}));if(!res.ok||!data.user){toast(data.error||'Invalid username or password');return}setRole(data.user.role);setLogged(true);localStorage.setItem('noor_user',data.user.username);localStorage.setItem('noor_role',data.user.role);toast(`Welcome, ${data.user.role==='admin'?'Admin':'Staff'}`);r.push('/admin/dashboard')}catch{toast('Login failed. Check your internet connection.')}finally{setBusy(false)}}return <main className="login-page"><div className="login-modal"><button className="modal-x" onClick={()=>r.push('/')}><X/></button><div className="modal-logo"><Logo small/></div><div className="login-brand">Travel Story</div><h2>Welcome Back</h2><p>Login to manage your products</p><form onSubmit={submit}><label>Username<input value={u} onChange={e=>setU(e.target.value)} placeholder="Enter username" autoComplete="username"/></label><label>Password<input type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="Enter password" autoComplete="current-password"/></label><button className="submit-btn" disabled={busy}><LockKeyhole size={16}/>{busy?'Signing in...':'Login'}</button></form></div></main>}
