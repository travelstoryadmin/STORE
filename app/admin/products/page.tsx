'use client';
import {useRef,useState} from 'react';
import {Pencil,Plus,Search,Trash2,Upload,Image as ImageIcon,X,RefreshCw} from 'lucide-react';
import {categories,Product,money,seedProducts} from '@/lib/data';
import {useStore} from '@/lib/store';
import {Panel,Toolbar} from '@/components/Panel';
const blank:Product={id:0,name:'',category:categories[0],price:0,expense:0,stock:0,image:'',active:true};
export default function Products(){
 const{products,setProducts,toast,role}=useStore();
 const[form,setForm]=useState<Product>(blank);const[edit,setEdit]=useState<number|null>(null);const[q,setQ]=useState('');const fileRef=useRef<HTMLInputElement>(null);
 const data=products.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q.toLowerCase()));
 function save(){if(!form.name||!form.price){toast('Enter product name and price');return}if(edit!==null)setProducts(v=>v.map(p=>p.id===edit?{...form,id:edit}:p));else setProducts(v=>[...v,{...form,id:Date.now()}]);setForm(blank);setEdit(null);toast(edit!==null?'Product updated':'Product added')}
 function remove(id:number){if(confirm('Delete this product?')){setProducts(v=>v.filter(p=>p.id!==id));toast('Product deleted')}}
 function chooseImage(file?:File){if(!file)return;if(!file.type.startsWith('image/')){toast('Please select an image file');return}if(file.size>3*1024*1024){toast('Image must be 3MB or smaller');return}const reader=new FileReader();reader.onload=()=>setForm(v=>({...v,image:String(reader.result||'')}));reader.readAsDataURL(file)}
 function restoreDemo(){if(confirm('Add the demo products again? Existing products will be kept.')){setProducts(v=>{const existing=new Set(v.map(p=>p.name.toLowerCase()));const add=seedProducts.filter(p=>!existing.has(p.name.toLowerCase())).map(p=>({...p,id:Date.now()+Math.floor(Math.random()*100000)}));return [...v,...add]});toast('Demo products restored')}}
 return <><Toolbar title="Products" subtitle="Add, edit and manage products shown on the customer homepage"/><div className="two-col"><Panel><h3>{edit!==null?'Edit Product':'Add New Product'}</h3><div className="form-grid"><label>Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label>Product Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Premium Oud"/></label><label>Selling Price (₹)<input type="number" value={form.price||''} onChange={e=>setForm({...form,price:Number(e.target.value)})}/></label>{role==='admin'&&<label>Expense / Cost (₹)<input type="number" value={form.expense||''} onChange={e=>setForm({...form,expense:Number(e.target.value)})}/></label>}<label>Stock Quantity<input type="number" value={form.stock||''} onChange={e=>setForm({...form,stock:Number(e.target.value)})}/></label><label>Image URL <span className="muted-label">(optional)</span><input value={form.image.startsWith('data:')?'':form.image} onChange={e=>setForm({...form,image:e.target.value})} placeholder="https://..."/></label></div><div className="product-image-upload" onClick={()=>fileRef.current?.click()}><input ref={fileRef} type="file" accept="image/png,image/jpeg,image/webp,image/gif" hidden onChange={e=>chooseImage(e.target.files?.[0])}/>{form.image?<div className="image-preview-wrap"><img src={form.image} className="product-preview" alt="Product preview"/><button type="button" className="remove-image" onClick={e=>{e.stopPropagation();setForm(v=>({...v,image:''}));if(fileRef.current)fileRef.current.value=''}}><X size={15}/></button><div className="upload-overlay"><Upload size={18}/> Change image</div></div>:<><div className="upload-icon"><ImageIcon size={28}/><Upload size={15}/></div><b>Upload Product Image</b><span>Click to choose JPG, PNG, WEBP or GIF</span><small>Maximum 3MB</small></>}</div><div className="modal-actions"><button onClick={()=>{setForm(blank);setEdit(null);if(fileRef.current)fileRef.current.value=''}}>Clear</button><button className="primary-btn" onClick={save}><Plus/> {edit!==null?'Update Product':'Add Product'}</button></div></Panel><Panel><div className="filterbar"><div className="search-box dark"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products..."/></div></div>{data.length===0?<div className="empty-state"><div className="empty-icon"><ImageIcon size={28}/></div><h3>No products found</h3><p>Your demo products may have been cleared from this browser.</p><button className="primary-btn" onClick={restoreDemo}><RefreshCw size={16}/> Restore Demo Products</button></div>:<div className="table-card"><table><thead><tr><th>Image</th><th>Name</th><th>Category</th><th>Price</th>{role==='admin'&&<th>Expense</th>}<th>Stock</th><th>Action</th></tr></thead><tbody>{data.map(p=><tr key={p.id}><td>{p.image?<img className="thumb" src={p.image} alt={p.name}/>:<div className="thumb-placeholder"><ImageIcon size={16}/></div>}</td><td><b>{p.name}</b></td><td>{p.category}</td><td>{money(p.price)}</td>{role==='admin'&&<td>{money(p.expense)}</td>}<td>{p.stock}</td><td><div className="row-actions"><button onClick={()=>{setForm(p);setEdit(p.id)}}><Pencil size={15}/></button><button className="danger" onClick={()=>remove(p.id)}><Trash2 size={15}/></button></div></td></tr>)}</tbody></table></div>}</Panel></div></>}
