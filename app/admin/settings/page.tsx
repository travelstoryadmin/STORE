'use client';import {Save,ShieldCheck,Trash2,LockKeyhole} from 'lucide-react';import {useStore} from '@/lib/store';import {Panel,Toolbar} from '@/components/Panel';export default function Settings(){const{role,products,customers,bills,setProducts,setCustomers,setBills,setStockLogs,toast}=useStore();function reset(){if(confirm('Reset all Travel Story demo data?')){localStorage.removeItem('noor_products_v3');localStorage.removeItem('noor_customers_v3');localStorage.removeItem('noor_bills_v3');localStorage.removeItem('noor_stock_logs_v3');setProducts([]);setCustomers([]);setBills([]);setStockLogs([]);toast('Demo data reset')}}return <><Toolbar title="Settings" subtitle="Store and account preferences"/><div className="settings-grid"><Panel><h3>Store Settings</h3><label>Store Name<input defaultValue="Travel Story"/></label><label>WhatsApp Number<input defaultValue="919999999999"/></label><label>Currency<input defaultValue="INR (₹)"/></label><button className="primary-btn"><Save/>Save Settings</button></Panel><Panel><h3><ShieldCheck/> Account & Access</h3><div className="access"><b>Logged in as</b><span>{role==='admin'?'Administrator':'Staff Member'}</span></div><div className="privacy"><LockKeyhole/><span>Expense and profit values are visible only to Admin.</span></div>{role==='admin'&&<button className="danger-wide" onClick={reset}><Trash2/>Reset Demo Data</button>}</Panel><Panel><h3>Data Overview</h3><div className="overview"><span>Products <b>{products.length}</b></span><span>Customers <b>{customers.length}</b></span><span>Bills <b>{bills.length}</b></span></div></Panel></div></>}
