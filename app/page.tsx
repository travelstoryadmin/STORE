'use client';
import {useState} from 'react';import Link from 'next/link';import {LockKeyhole,MessageCircle,Search} from 'lucide-react';import Logo from '@/components/Logo';import {categories,Product,WA_NUMBER} from '@/lib/data';import {useStore} from '@/lib/store';
export default function Home(){const{products}=useStore();const[q,setQ]=useState('');const filtered=products.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q.toLowerCase()));function wa(p:Product){const text=`Hello Travel Story, I am interested in ${p.name}. Price: ₹${p.price}.`;window.open(`https://wa.me/${WA_NUMBER}?text=${encodeURIComponent(text)}`,'_blank')}return <main className="public"><header className="public-head"><Logo/><nav><Link className="active" href="/">Home</Link><a>Shop</a><a>Categories</a><a>About</a><a>Contact</a></nav><div className="public-actions"><div className="search-box"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search for products..."/><Search size={18}/></div><Link className="login-btn" href="/login"><LockKeyhole size={17}/>Login</Link></div></header><section className="hero"><div className="hero-copy"><small>PURE SCENTS<br/>BEAUTIFUL MOMENTS</small><h1>Fragrance<br/><span>that Touches</span><br/>Your Heart</h1><p>Premium Attars, Gifts & More<br/>for Every Special Moment</p><button onClick={()=>document.getElementById('products')?.scrollIntoView({behavior:'smooth'})}>Explore Products →</button></div><div className="hero-photo"><div className="hero-bottle">✦</div><div className="hero-bottle b2">◇</div><i>More than a Fragrance<br/>It's a Feeling...</i></div></section><section className="category-row">{categories.map(c=><button key={c} onClick={()=>setQ(c)}><span>✦</span><b>{c}</b></button>)}<button onClick={()=>setQ('')}><span>⠿</span><b>All Products</b></button></section><section className="offer"><div><small>SPECIAL OFFER</small><h2>Meaningful Gifts<br/>for Your Loved Ones</h2><p>Beautiful Items for Every Occasion</p></div><button onClick={()=>setQ('Gift')}>Shop Now →</button></section><section id="products" className="public-products"><div className="section-head"><h2>All Products</h2><span>{filtered.length} products</span></div><div className="public-grid">{filtered.map(p=><article className="public-card" key={p.id}><img src={p.image} alt={p.name}/><div><b>{p.name}</b><small>{p.category} · ₹{p.price.toLocaleString('en-IN')}</small><button onClick={()=>wa(p)}><MessageCircle size={16}/>Chat on WhatsApp</button></div></article>)}</div></section><footer><Logo small/><div className="footer-links"><a>Home</a><a>Shop</a><a>About</a><a>Contact</a><a>Privacy Policy</a></div><span>© 2026 Travel Story. All rights reserved.</span></footer></main>}
